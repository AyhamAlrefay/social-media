
name: whatsapp
description: A new Flutter project.


publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.19.0-146.1.beta <3.0.0'


dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2
  firebase_core:
  country_picker:
  firebase_auth:
  firebase_messaging:
  firebase_storage:
  cloud_firestore:
  equatable:
  flutter_bloc:
  get_it:
  emoji_picker_flutter:
  image_picker:
  bloc: 
  dartz:
  swipe_to:
  change_app_package_name: ^1.1.0
  flutter_contacts: ^1.1.5
  file_picker:
  path_provider:
  uuid:
  open_file: ^3.2.0
  camera:
  video_player:



dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^2.0.0


flutter:


  uses-material-design: true


  assets:
    - assets/images/

